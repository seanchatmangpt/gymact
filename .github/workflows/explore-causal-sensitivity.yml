name: EXPLORE causal sensitivity exact head
on:
  pull_request:
    paths:
      - 'src/gymact/explore_causal_sensitivity/**'
      - 'tests/explore_causal_sensitivity/**'
      - '.github/workflows/explore-causal-sensitivity.yml'
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - uses: astral-sh/setup-uv@v7
      - run: uv sync --group dev
      - run: uv run python -m compileall -q src/gymact/explore_causal_sensitivity tests/explore_causal_sensitivity
      - run: uv run python -m unittest discover -s tests/explore_causal_sensitivity -p 'test_*.py' -v
      - run: uv run ruff check src/gymact/explore_causal_sensitivity tests/explore_causal_sensitivity
      - run: uv run ruff format --check src/gymact/explore_causal_sensitivity tests/explore_causal_sensitivity
      - run: "! grep -REn 'requests\\.(post|put|patch|delete)|subprocess\\.|os\\.system|boto3|azure\\.|google\\.cloud' src/gymact/explore_causal_sensitivity"
