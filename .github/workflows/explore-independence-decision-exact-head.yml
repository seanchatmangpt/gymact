name: EXPLORE Independence Decision Exact Head

on:
  pull_request:
    paths:
      - 'src/gymact/explore_independence_decision/**'
      - 'tests/explore_independence_decision/**'
      - '.github/workflows/explore-independence-decision-exact-head.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.sha }}
      - name: Assert exact pull-request head
        shell: bash
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - uses: astral-sh/setup-uv@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.13'
      - name: Close declared dev dependencies
        run: uv sync --group dev
      - name: Compile
        run: uv run python -m compileall -q src/gymact/explore_independence_decision tests/explore_independence_decision
      - name: Behavioral and Chicago courts
        run: uv run pytest -q tests/explore_independence_decision
      - name: Static conformance
        run: uv run ruff check src/gymact/explore_independence_decision tests/explore_independence_decision
      - name: Format conformance
        run: uv run ruff format --check src/gymact/explore_independence_decision tests/explore_independence_decision
      - name: No ambient consequential actuation
        shell: bash
        run: |
          ! grep -REn 'requests\.(post|put|patch|delete)|subprocess\.|os\.system|boto3|azure\.|google\.cloud' src/gymact/explore_independence_decision
