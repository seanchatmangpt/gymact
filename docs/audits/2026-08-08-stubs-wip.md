# Stub / WIP Audit — 2026-08-08

Status: `PARTIAL_ALIVE`

## Admitted subject

- Repository: `seanchatmangpt/gymact`
- Base: `main`
- Observed tree SHA: `4a42f3a9f1bd3c20fbbd3babd5c66125b1a1492d`
- Source package observed under `src/gymact/`

## Confirmed findings

### WIP-001 — Release version exists without any repository tag

The committed release-checklist design states that `pyproject.toml` and `CHANGELOG.md` say `26.8.7`, but PR #2 set that version without the release pipeline firing. The GitHub tags endpoint currently returns an empty set.

**Classification:** `INCOMPLETE_WIP / RELEASE_STANDING`

A version string without the corresponding immutable release/tag identity leaves the release state incomplete and weakens replay/provenance.

**Recommended closure:** execute the repository's admitted release path after current pre-flight verification and confirm tag, GitHub release, PyPI artifact, and scratch-install smoke test all bind to the same version/source identity.

### WIP-002 — Committed release plan records unlanded provider work

`docs/superpowers/specs/2026-08-07-v26.8.7-release-checklist-design.md` records four providers as “fully implemented and tested but entirely uncommitted,” plus a modified fixture and an untracked worktree directory.

**Classification:** `INCOMPLETE_WIP / SOURCE_STANDING`

This is explicit evidence that the release scope described by the repository was not fully represented by the committed canonical tree at the time the plan was written. A committed plan referring to uncommitted implementation is not itself proof that those providers have standing.

**Recommended closure:** reconcile the plan against the current tree: either land each admitted provider with its tests/fixtures and verification receipt, or revise the plan to mark it out of scope/obsolete with provenance.

### WIP-003 — Known standing model is explicitly non-general

The same release checklist warns that `ocel_standing.py`'s standing model “doesn't yet” generalize beyond the `discovered` family.

**Classification:** `INCOMPLETE_WIP / SEMANTIC_COVERAGE`

This is a bounded, explicit capability gap rather than an inferred defect.

**Recommended closure:** either generalize standing semantics to the intended provider families with fixtures and negative controls, or encode/refuse unsupported families explicitly so callers cannot mistake partial coverage for general standing.

## Search limitation

Repository code-search requests for TODO/stub markers returned upstream 502 during this audit. The exact tree, package surface, release-plan evidence, and repository tags were directly observed; exhaustive content scanning of every Python blob was not completed through that transport.

## Standing

`PARTIAL_ALIVE`: exact repository state and three explicit incompleteness findings are grounded; exhaustive marker coverage remains incomplete.
