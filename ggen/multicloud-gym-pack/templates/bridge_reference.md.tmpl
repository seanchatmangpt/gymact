---
to: "docs/multicloud-gym/reference.md"
sparql:
  ops: |
    PREFIX dct: <http://purl.org/dc/terms/>
    PREFIX sosa: <http://www.w3.org/ns/sosa/>
    SELECT ?id ?consequence_class
    WHERE {
      ?cap a sosa:Procedure ;
           dct:title ?id ;
           dct:type ?consequence_class_iri .
      BIND(IF(CONTAINS(STR(?consequence_class_iri), "consequence:do"), "Do", "Read") AS ?consequence_class)
    }
    ORDER BY ?id
---
# GymAct Bridge — Operation Reference

GENERATED by `ggen sync` from this pack's own `ontology.ttl`. Do not edit
by hand: this report is a pure SPARQL projection of your admitted graph.

**Provenance**: this pack's `sosa:Procedure` capability facts are YOUR
system's admitted data — the real capabilities your `EnvironmentProvider`
implementation actually exposes to GymAct (see `docs/integrations/` in
the `gymact` repo for how a provider is registered), validated against
GymAct's own REAL SHACL shape (`shapes/profile.shacl.ttl`, fetched via
`gymact export-profile` — never hand-copied or re-derived —
`urn:gymact:shape:capability`). This is not GymAct's own built-in
operation taxonomy; it is your system's capability surface, expressed the
same way GymAct expresses its own, so both sides speak one shared
vocabulary. It carries no authority-related facts: GymAct's authority
machinery is Python-runtime-only and not yet represented in RDF at all
(see `~/gymact/.claude/rules/actuation-authority.md`) — keep your own
authority decisions in your runtime, not in this graph.

## Operations ({{ ops | length }})

| id | Consequence class |
|---|---|
{% for row in ops -%}
| `{{ row.id }}` | {{ row.consequence_class }} |
{% endfor %}

## The one invariant this bridge preserves

Every admitted capability is a `sosa:Procedure` classified `Read`
(sosa:Observation-shaped) or `Do` (sosa:Actuation-shaped) via
`dct:type` — enforced at sync time by GymAct's own real SHACL shape
(`urn:gymact:shape:capability`, wired via this pack's `shape:` template
frontmatter), not a bridge-invented constraint. A capability whose
`dct:type` falls outside the admitted
`{urn:gymact:consequence:read, urn:gymact:consequence:do}` set is refused
by that real shape before any output is written — the same fail-closed
rule GymAct enforces on its own capability data.
