package gymact:togaf-gym@0.1.0;

interface architecture-gym {
    record task {
        id: string,
        title: string,
        phase: string,
        family: string,
        oracle: string,
    }

{% for row in results %}{% if row["recordKind"] == "task" %}    // admitted-task: {{ row["id"] }}
{% endif %}{% endfor %}    list-tasks: func() -> list<task>;
    evaluate-rdf-submission: func(task-id: string, turtle: string) -> string;
}

world togaf-gym {
    export architecture-gym;
}
