# Compiled Enterprise-Architecture Gym Reference

This document is a deterministic projection of the admitted public-ontology graph. It is not TOGAF documentation and contains no TOGAF body text.

## Public semantic model

`PROF + PROV-O + ORG + SKOS + P-PLAN + OSLC RM + DCAT + ODRL + SHACL + EARL`

## Admitted ADM labels

| Notation | Public label |
|---|---|
{% for row in results %}{% if row["recordKind"] == "phase" %}| {{ row["notation"] }} | {{ row["label"] }} |
{% endif %}{% endfor %}
## Gym tasks

| Task | Phase | Family | Oracle |
|---|---|---|---|
{% for row in results %}{% if row["recordKind"] == "task" %}| `{{ row["id"] }}` | {{ row["phase"] }} | {{ row["family"] }} | `{{ row["oracle"] }}` |
{% endif %}{% endfor %}
## Law

TOGAF-specific vocabulary is classification data. Public ontologies own semantics. SHACL/EARL own task falsification. SPARQL gates own ggen admission. `ggen` owns deterministic projection and its generation receipt. Runtime actuation, if a future task requires it, remains behind GymAct/BRCE authority rather than this pack.
