---
to: "src/gymact/gyms/cloud_topology_gym.py"
force: true
sparql:
  capabilities: "PREFIX cv: <http://seanchatmangpt.github.io/packs/cloud-topology-validation#> SELECT ?capabilityIri ?capabilityTitle ?capabilityBinding ?capabilityDeclaredCostUsd ?capabilityDeclaredCostSource WHERE { ?c a cv:GymCapability ; cv:capabilityIri ?capabilityIri ; cv:capabilityTitle ?capabilityTitle ; cv:capabilityBinding ?capabilityBinding ; cv:capabilityOrder ?capabilityOrder . OPTIONAL { ?c cv:capabilityDeclaredCostUsd ?capabilityDeclaredCostUsd ; cv:capabilityDeclaredCostSource ?capabilityDeclaredCostSource } } ORDER BY ?capabilityOrder"
  providerCosts: "PREFIX cv: <http://seanchatmangpt.github.io/packs/cloud-topology-validation#> SELECT ?providerId ?managedK8sHourlyCostUsd ?managedK8sCostSource WHERE { ?p a cv:CloudProvider ; cv:providerId ?providerId ; cv:managedK8sHourlyCostUsd ?managedK8sHourlyCostUsd ; cv:managedK8sCostSource ?managedK8sCostSource } ORDER BY ?providerId"
---
"""Real, read-only gym exposing `gymact.gyms.cloud_topology`'s
provider-published cloud data as a queryable `Environment` -- so an agent
with no prior cloud knowledge can ask "what regions/services does this
provider actually have" and get the real, published answer, not a
simulated approximation (see `cloud_topology.py`'s own module docstring for
why `multicloud.py`/`cloudsim` don't already provide this).

Every capability here is `Consequence.READ` -- this gym never actuates
anything; it only answers questions against the real, already-loaded
topology data. `requires_authority` defaults `False`: reading a cloud
provider's own public region/service catalog carries no real consequence
to gate.

GENERATED by ggen from ggen/cloud-topology-validation-pack/ontology.ttl -- do not hand-edit.
The capability catalog (iri/title/binding, in cv:capabilityOrder) is echoed from that
ontology's cv:GymCapability individuals; adding a capability means adding an individual
there (and, if it needs genuinely new actuate() logic, a new branch in this pack's
templates/cloud_topology_gym.py.tmpl), never hand-editing this file directly.
"""

from __future__ import annotations

from dataclasses import asdict
from typing import Any
from uuid import uuid4

from gymact.models import Capability, Consequence, CostDimension

from . import cloud_topology_validation
from .cloud_topology import CloudTopology, load_topology
from .cloud_topology_validation import ValidationResult

CLOUD_TOPOLOGY_CAPABILITIES: tuple[Capability, ...] = (
{% for c in capabilities %}    Capability(
        iri="{{ c.capabilityIri }}",
        title="{{ c.capabilityTitle }}",
        consequence=Consequence.READ,
        binding="{{ c.capabilityBinding }}",
{% if c.capabilityDeclaredCostUsd %}        costs=(
            CostDimension(
                unit="usd",
                quantity={{ c.capabilityDeclaredCostUsd }},
                kind="declared_estimate",
                source="{{ c.capabilityDeclaredCostSource }}",
            ),
        ),
{% endif %}    ),
{% endfor %})
_CAPABILITY_BY_BINDING = {c.binding: c for c in CLOUD_TOPOLOGY_CAPABILITIES}

#: Real, ontology-sourced managed-Kubernetes hourly control-plane cost per
#: provider -- echoed from cv:CloudProvider's cv:managedK8sHourlyCostUsd.
_MANAGED_K8S_COST_USD: dict[str, float] = {
{% for p in providerCosts %}    "{{ p.providerId }}": {{ p.managedK8sHourlyCostUsd }},
{% endfor %}}
_MANAGED_K8S_COST_SOURCE: dict[str, str] = {
{% for p in providerCosts %}    "{{ p.providerId }}": "{{ p.managedK8sCostSource }}",
{% endfor %}}


class CloudTopologyEnvironment:
    """A materialized, real, provider-published cloud topology for one
    real provider (`"aws"`, `"azure"`, or `"gcp"`)."""

    def __init__(self, *, provider: str, requires_authority: bool = False) -> None:
        self.environment_id = f"urn:gymact:cloud-topology:environment:{uuid4().hex}"
        self.requires_authority = requires_authority
        self.provider = provider
        self._topology: CloudTopology = load_topology(provider)
        self._closed = False

    def _ensure_open(self) -> None:
        if self._closed:
            raise RuntimeError("environment is torn down")

    def capabilities(self) -> tuple[Capability, ...]:
        self._ensure_open()
        return CLOUD_TOPOLOGY_CAPABILITIES

    def _validate(self) -> ValidationResult:
        validator = getattr(cloud_topology_validation, f"validate_{self.provider}")
        return validator()

    def _state(self) -> dict[str, Any]:
        result = self._validate()
        return {
            "provider": self._topology.provider,
            "region_count": len(self._topology.regions),
            "service_count": len(self._topology.services),
            "source_url": self._topology.source_url,
            "source_version": self._topology.source_version,
            "fetched_at": self._topology.fetched_at,
            "validated": result.valid,
            "stale": result.stale,
        }

    async def observe(self) -> dict[str, Any]:
        self._ensure_open()
        return self._state()

    async def actuate(self, capability: Capability, payload: dict[str, Any]) -> dict[str, Any]:
        self._ensure_open()
        before = self._state()
        binding = capability.binding
{% for c in capabilities %}{% if c.capabilityBinding == "list_regions" %}        if binding == "list_regions":
            result: Any = list(self._topology.region_codes())
{% elif c.capabilityBinding == "list_services" %}        elif binding == "list_services":
            result = list(self._topology.service_names())
{% elif c.capabilityBinding == "services_in_region" %}        elif binding == "services_in_region":
            region = payload.get("region")
            if not isinstance(region, str) or not region:
                raise ValueError("payload.region must be a non-empty string")
            result = list(self._topology.services_in_region(region))
{% elif c.capabilityBinding == "validate_topology" %}        elif binding == "validate_topology":
            result = asdict(self._validate())
{% elif c.capabilityBinding == "estimated_managed_k8s_cost" %}        elif binding == "estimated_managed_k8s_cost":
            result = {
                "provider": self.provider,
                "unit": "usd",
                "quantity_per_hour": _MANAGED_K8S_COST_USD.get(self.provider),
                "source": _MANAGED_K8S_COST_SOURCE.get(self.provider),
            }
{% endif %}{% endfor %}        else:
            raise ValueError(f"unsupported cloud-topology binding: {binding}")
        return {
            "before": before,
            "after": self._state(),
            "capability": capability.iri,
            "result": result,
        }

    async def verify(self, expected: dict[str, Any]) -> tuple[bool, dict[str, Any]]:
        self._ensure_open()
        observed = self._state()
        passed = all(observed.get(key) == value for key, value in expected.items())
        return passed, observed

    async def checkpoint(self) -> dict[str, Any]:
        self._ensure_open()
        # The real topology is immutable once loaded (read-only gym, no
        # actuation mutates it) -- a checkpoint has nothing real to capture
        # beyond identity.
        return {"provider": self.provider}

    async def restore(self, checkpoint: dict[str, Any]) -> None:
        self._ensure_open()
        if checkpoint.get("provider") != self.provider:
            raise ValueError("checkpoint belongs to a different provider")

    async def teardown(self) -> None:
        self._closed = True


class CloudTopologyProvider:
    """GymAct `EnvironmentProvider` materializing a real,
    provider-published `CloudTopologyEnvironment`."""

    name = "cloud-topology"
    materialization_requires_authority = False

    async def materialize(
        self, *, scenario: str | None, config: dict[str, Any]
    ) -> CloudTopologyEnvironment:
        provider = scenario or config.get("provider", "aws")
        if not isinstance(provider, str):
            raise TypeError("config.provider must be a string")
        requires_authority = config.get("requires_authority", False)
        if not isinstance(requires_authority, bool):
            raise TypeError("config.requires_authority must be a boolean")
        return CloudTopologyEnvironment(provider=provider, requires_authority=requires_authority)
