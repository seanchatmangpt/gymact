---
to: "src/sregym_e2e_contract.rs"
sparql:
  contract: |
    PREFIX dct: <http://purl.org/dc/terms/>
    SELECT (STR(?source) AS ?repository) ?revision WHERE {
      <urn:gymact:sregym:e2e:contract>
        dct:source ?source ;
        dct:hasVersion ?revision ;
        dct:conformsTo <urn:gymact:mcp:revision-2026-07-28> .
    }
  capability_count: |
    PREFIX sosa: <http://www.w3.org/ns/sosa/>
    PREFIX sr: <urn:gymact:sregym:mcp:>
    SELECT (COUNT(DISTINCT ?cap) AS ?count) WHERE { ?cap a sosa:Procedure, sr:Capability . }
  read_count: |
    PREFIX sosa: <http://www.w3.org/ns/sosa/>
    PREFIX sr: <urn:gymact:sregym:mcp:>
    SELECT (COUNT(DISTINCT ?cap) AS ?count) WHERE { ?cap a sosa:Procedure, sr:Capability ; sr:consequence sr:READ . }
  do_count: |
    PREFIX sosa: <http://www.w3.org/ns/sosa/>
    PREFIX sr: <urn:gymact:sregym:mcp:>
    SELECT (COUNT(DISTINCT ?cap) AS ?count) WHERE { ?cap a sosa:Procedure, sr:Capability ; sr:consequence sr:DO . }
  corpus_count: |
    PREFIX sr: <urn:gymact:sregym:mcp:>
    SELECT (COUNT(DISTINCT ?subject) AS ?count) WHERE { ?subject a sr:CorpusSubject . }
  program_count: |
    PREFIX sr: <urn:gymact:sregym:mcp:>
    SELECT (COUNT(DISTINCT ?program) AS ?count) WHERE { ?program a sr:Program . }
---
//! GENERATED by ggen from the admitted SREGym MCP ontology.
//!
//! This crate is a powerless structural verifier. It cannot crown runtime
//! ALIVE standing: no subprocess, MCP transport, Kubernetes, credentials,
//! model, AuthorityResolver or BRCE execution API exists here.

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct StructuralContract {
    pub repository: &'static str,
    pub revision: &'static str,
    pub capability_count: usize,
    pub read_count: usize,
    pub do_count: usize,
    pub corpus_count: usize,
    pub deterministic_program_count: usize,
}

pub const CONTRACT: StructuralContract = StructuralContract {
    repository: "{{ contract[0].repository }}",
    revision: "{{ contract[0].revision }}",
    capability_count: {{ capability_count[0].count }},
    read_count: {{ read_count[0].count }},
    do_count: {{ do_count[0].count }},
    corpus_count: {{ corpus_count[0].count }},
    deterministic_program_count: {{ program_count[0].count }},
};

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct ObservedStructure<'a> {
    pub repository: &'a str,
    pub revision: &'a str,
    pub capability_count: usize,
    pub read_count: usize,
    pub do_count: usize,
    pub corpus_count: usize,
    pub deterministic_program_count: usize,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum StructuralDisposition {
    Admitted,
    Refused,
}

pub fn verify_structure(observed: ObservedStructure<'_>) -> StructuralDisposition {
    if observed.repository == CONTRACT.repository
        && observed.revision == CONTRACT.revision
        && observed.capability_count == CONTRACT.capability_count
        && observed.read_count == CONTRACT.read_count
        && observed.do_count == CONTRACT.do_count
        && observed.corpus_count == CONTRACT.corpus_count
        && observed.deterministic_program_count == CONTRACT.deterministic_program_count
    {
        StructuralDisposition::Admitted
    } else {
        StructuralDisposition::Refused
    }
}

/// Static generation never implies runtime execution standing.
pub const STRUCTURAL_ADMISSION_IS_ALIVE: bool = false;
